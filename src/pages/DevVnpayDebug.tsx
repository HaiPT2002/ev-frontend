import React from 'react';

export default function DevVnpayDebug() {
  return (
    <div>
      <h2>VNPAY Debug (dev only)</h2>
      <p>
        Show canonical string and signatures; call backend debug endpoints. Protect by ADMIN role.
      </p>
    </div>
  );
}
